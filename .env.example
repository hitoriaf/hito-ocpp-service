# Copy this file to .env and adjust as needed

# Database configuration (for local dev)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=voltron_db
POSTGRES_USER=voltron_user
POSTGRES_PASSWORD=voltron_password

# When using docker-compose, these are used by the DB container and ocpp-server container
# You can override POSTGRES_PORT to expose DB on host
# Example: POSTGRES_PORT=5433

# Redis configuration (for queue management)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Server
PORT=8080
NODE_ENV=development
